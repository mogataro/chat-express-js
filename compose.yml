services:
  app: &app
    image: node:lts-slim
    working_dir: /app
    volumes:
      - ./app:/app:cached
    ports:
      - 3000:3000
      - 8080:8080
    tty: true

  setup:
    image: busybox
    depends_on:
      - setup-node_module
  setup-node_module:
    <<: *app
    command: cp .env.example .env && npm ci
